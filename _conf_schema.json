{
  "comfyui_servers": {
    "description": "ComfyUI 服务器列表",
    "hint": "可添加多个服务器，但同一时间只能启用一个（enabled=true）。绘图时自动选用启用的那一个；若都未启用则使用第一个。点击右上角“添加”新增条目。",
    "type": "template_list",
    "templates": {
      "default": {
        "name": "服务器",
        "display_item": "name",
        "items": {
          "name": {
            "type": "string",
            "description": "服务器名称",
            "hint": "自定义标识，用于工作流绑定（server_name 填它）。",
            "default": "server1"
          },
          "url": {
            "type": "string",
            "description": "服务地址",
            "hint": "需包含 http(s)://，例如 http://127.0.0.1:8188",
            "default": "http://127.0.0.1:8188"
          },
          "enabled": {
            "type": "bool",
            "description": "是否启用",
            "hint": "多个服务器只能启用一个，否则绘图会报错。",
            "default": false
          },
          "client_id": {
            "type": "string",
            "description": "客户端 ID（可选）",
            "hint": "用于区分队列中的任务，留空会自动生成。",
            "default": ""
          }
        }
      }
    },
    "default": []
  },
  "loras": {
    "description": "LoRA 库（全局，所有工作流共用）",
    "hint": "在此集中管理每个 LoRA 的真实文件名、是否「仅模型」、关键词与多套提示词预设。工作流里只需用 名称|权重|是否启用 引用即可。点击“添加”新增 LoRA。",
    "type": "template_list",
    "templates": {
      "default": {
        "name": "LoRA",
        "display_item": "name",
        "items": {
          "name": {
            "type": "string",
            "description": "LoRA 名称（引用键）",
            "hint": "在工作流与指令里都用这个名字引用，例如 --安魂曲。",
            "default": ""
          },
          "base_model": {
            "type": "string",
            "description": "底模（基座模型）",
            "hint": "该 LoRA 适用的底模类型，用于与工作流底模匹配、让 LLM 正确选 LoRA。可选：anima / z-image-turbo / krea2 / illustrious；留空 = 通用（任何底模都可用）。",
            "default": "",
            "options": ["", "anima", "z-image-turbo", "krea2", "illustrious"]
          },
          "model_name": {
            "type": "string",
            "description": "模型文件名",
            "hint": "LoRA 的模型文件名（.safetensors），例如 anima_lacrimosa.safetensors。/draw --名称 就是按这个名字去 ComfyUI 加载对应的 LoRA 文件。",
            "default": ""
          },
          "model_only": {
            "type": "bool",
            "description": "使用「仅模型」节点",
            "hint": "默认开启。开启时只把 LoRA 叠加到去噪网络（MODEL），不需要 CLIP 锚点、兼容性最好；某些需要文本编码器权重的 LoRA（如部分风格 LoRA）若要同时影响 CLIP，请关闭此项。",
            "default": true
          },
          "weight": {
            "type": "float",
            "description": "默认权重",
            "hint": "指令 / 工作流未指定权重时使用。",
            "default": 1.0
          },
          "keywords": {
            "type": "string",
            "description": "别名（触发关键词，可选）",
            "hint": "逗号分隔。这是该 LoRA 的别名/触发词，用户输入提示词命中这些词时自动启用该 LoRA（即便工作流默认未启用）；LLM 也用这些别名来区分和引用 LoRA。",
            "default": ""
          },
          "trigger_words": {
            "type": "text",
            "description": "触发词（多行，可选）",
            "hint": "该 LoRA 的完整触发词列表，每行一个（C 站风格类 LoRA 常有多个，如 sketch / monochrome / greyscale）。填写 C 站链接点「抓取」可自动填入。主要用于给 LLM 理解该 LoRA 怎么用、以及展示。",
            "default": ""
          },
          "description": {
            "type": "text",
            "description": "LoRA 描述（可选）",
            "hint": "用一句话描述这个 LoRA 的用途/效果（如「白色水手服少女风格」「复古胶片色调」）。LLM 调用该 LoRA 前会读取此描述来理解其用途。填写 C 站链接点「抓取」可自动填入。",
            "default": ""
          },
          "civitai_url": {
            "type": "string",
            "description": "C 站链接（可选）",
            "hint": "Civitai (C站) 模型的页面链接，如 https://civitai.com/models/12345。在 WebUI 中填写后点「抓取」按钮，可自动下载封面图到本地并填入触发词/描述；网络不可用时请手动上传图片、填写描述与触发词。",
            "default": ""
          },
          "image": {
            "type": "string",
            "description": "封面图（本地文件名，可选）",
            "hint": "封面图文件名（存于插件数据目录 lora_assets/ 下）。通过 WebUI 抓取或上传后自动填充；WebUI 卡片展示该图，未设置时显示默认占位图。",
            "default": ""
          },
          "presets": {
            "type": "text",
            "description": "提示词预设（可多套）",
            "hint": "每套预设用 [预设名|提示词] 表示，多套之间用空格隔开，例如：[预设1|提示词1] [预设2|solo, 1girl]。提示词里可以包含逗号（中英文均可）。调用方式：/draw 提示词 --名称/预设名（名称与预设名之间用 / 分隔，以免和 LoRA 名字里常见的 - 冲突）。留空则无预设。⚠ 特殊：若某套预设的「预设名填 0」（如 [0|always on]），则该 LoRA 被启用时，这个 0 预设的提示词【每次都会自动带上】，无论你是否用别的预设、甚至完全不带预设都会生效（你已显式指定 --名称/0 时不会重复）。适合放「该 LoRA 必备、不希望被遗忘」的关键词。",
            "default": ""
          }
        }
      }
    },
    "default": []
  },
  "workflows": {
    "description": "工作流列表",
    "hint": "可添加多个工作流，每个工作流绑定一个服务器、配置节点 ID 与 LoRA。点击“添加”新增条目。",
    "type": "template_list",
    "templates": {
      "default": {
        "name": "工作流",
        "display_item": "name",
        "items": {
          "name": {
            "type": "string",
            "description": "工作流名称",
            "hint": "指令 /draw --wf 和 AI 调用时引用此名。",
            "default": "default"
          },
          "base_model": {
            "type": "string",
            "description": "底模（基座模型）",
            "hint": "本工作流使用的底模类型，用于与 LoRA 底模匹配（仅底模一致或 LoRA 通用时该 LoRA 对本工作流可用）。可选：anima / z-image-turbo / krea2 / illustrious；留空 = 不限制（所有 LoRA 都视为可用）。",
            "default": "",
            "options": ["", "anima", "z-image-turbo", "krea2", "illustrious"]
          },
          "aliases": {
            "type": "text",
            "description": "工作流别名",
            "hint": "外部传入的、会命中本工作流的其它名字（如伴侣插件传的 anime_selfie_workflow、ComfyUI default）。多个用逗号或换行隔开，每个别名一个。匹配大小写不敏感。",
            "default": ""
          },
          "image": {
            "type": "string",
            "description": "封面图（本地文件名，可选）",
            "hint": "封面图文件名（存于插件数据目录 lora_assets/ 下）。通过 WebUI 抓取或上传后自动填充；WebUI 卡片展示该图，未设置时显示默认占位图。",
            "default": ""
          },
          "civitai_url": {
            "type": "string",
            "description": "C 站链接（可选，用于抓取封面）",
            "hint": "Civitai 模型页面链接（含 ?modelVersionId= 会精确匹配版本）。WebUI 卡片点「抓取封面」可自动下载该模型的封面图到本地并填入 image。",
            "default": ""
          },
          "server_name": {
            "type": "string",
            "description": "绑定服务器",
            "hint": "填上面服务器的 name；只有一个服务器时可留空。",
            "default": ""
          },
          "is_anima": {
            "type": "bool",
            "description": "是否 Anima 工作流",
            "hint": "若为 true，提交前会把中文提示词翻译为英文标签（翻译模式见本工作流 translator_mode，或全局 translator_mode）。",
            "default": false
          },
          "translator_mode": {
            "type": "string",
            "description": "本工作流翻译模式（可选，覆盖全局）",
            "hint": "留空=跟随全局 translator_mode。可选：danbooru / llm / api，仅对 is_anima=true 的工作流生效。",
            "default": "",
            "options": ["", "danbooru", "llm", "api"]
          },
          "workflow_name": {
            "type": "string",
            "description": "工作流文件名",
            "hint": "插件数据目录 workflow/ 下的文件名（如 sd.json 或 sd）；只需填名字，不用写全路径，缺 .json 会自动补。也可改用最下方文本框直接粘贴 JSON。",
            "default": ""
          },
          "positive_node": {
            "type": "string",
            "description": "正向提示词节点 ID",
            "hint": "在导出的 JSON 里找 class_type 为 CLIPTextEncode 的正向提示词节点，把它的键名（如 6）填这里。正向下输入框名固定为 text，无需填写。",
            "obvious_hint": true,
            "default": ""
          },
          "negative_node": {
            "type": "string",
            "description": "负向提示词节点 ID",
            "hint": "负向 CLIPTextEncode 节点的键名（如 7）。负向下输入框名固定为 text，无需填写。",
            "obvious_hint": true,
            "default": ""
          },
          "resolution_node": {
            "type": "string",
            "description": "宽高节点 ID",
            "hint": "Empty Latent Image / 控制分辨率的节点键名（如 5）。宽和高属于同一节点，只需填这一个 ID。留空则自动查找工作流里的 EmptyLatentImage 节点。",
            "obvious_hint": true,
            "default": ""
          },
          "image_node": {
            "type": "string",
            "description": "参考图节点 ID（图生图）",
            "hint": "图生图时，把参考图注入到这个 LoadImage 节点的 image 输入。填 LoadImage 节点的键名（如 12）。留空则自动查找工作流里的 LoadImage 节点。",
            "obvious_hint": true,
            "default": ""
          },
          "resolution_width_field": {
            "type": "string",
            "description": "宽度输入框名",
            "hint": "宽高节点里控制宽度的输入框名称，一般是 width。",
            "default": "width"
          },
          "resolution_height_field": {
            "type": "string",
            "description": "高度输入框名",
            "hint": "宽高节点里控制高度的输入框名称，一般是 height。",
            "default": "height"
          },
          "output_node": {
            "type": "string",
            "description": "输出节点 ID（可选）",
            "hint": "SaveImage / PreviewImage 节点的键名；留空则自动取最后一个含 images 输出的节点。",
            "default": ""
          },
          "default_width": {
            "type": "int",
            "description": "默认宽度",
            "hint": "指令 / AI 未指定 --w 时使用。",
            "slider": {
              "min": 64,
              "max": 2048,
              "step": 64
            },
            "default": 512
          },
          "default_height": {
            "type": "int",
            "description": "默认高度",
            "hint": "指令 / AI 未指定 --h 时使用。",
            "slider": {
              "min": 64,
              "max": 2048,
              "step": 64
            },
            "default": 512
          },
          "default_denoise": {
            "type": "float",
            "description": "默认降噪幅度（denoise）",
            "hint": "图生图时控制重绘强度：1.0 = 完全重绘（几乎忽略原图），0 = 完全保留原图。文生图建议 1.0，图生图建议 0.55~0.8。填 -1 表示不注入（沿用工作流原始值）。",
            "slider": {
              "min": 0,
              "max": 1,
              "step": 0.05
            },
            "default": -1
          },
          "lora_anchor": {
            "type": "string",
            "description": "主模节点 ID（可选）",
            "hint": "工作流本身没有 LoraLoader 节点、想完全靠下方配置组装 LoRA 时填写：填主模加载节点（CheckpointLoader / UNETLoader）的键名（示例：4，仅举例、非默认值）。插件会在它之后自动新建 LoRA 节点并接线；留空则自动探测主模节点。若工作流已有 LoraLoader 则无需填写。默认「加载LoRA（仅模型）」只需这一路主模锚点即可。",
            "default": ""
          },
          "lora_clip": {
            "type": "string",
            "description": "主模 CLIP 节点 ID（可选，仅完整模式用）",
            "hint": "使用完整 LoraLoader（即该 LoRA 在全局库里关闭「仅模型」，同时影响 model+CLIP）时，若自动探测不到或接错 CLIP 源，在此显式填写 CLIP 模型节点的键名（示例：6，仅举例）。留空则自动探测。配合上方主模节点可分别指定 model 源与 clip 源，兼容「正向/负向提示词用不同 CLIPLoader」等工作流。仅模型模式不需要此项。",
            "default": ""
          },
          "loras_text": {
            "type": "text",
            "description": "LoRA 默认启用列表（每行一个，可选）",
            "hint": "每行一个 LoRA，用竖线 | 分隔三个字段：名称|权重|0/1（0=禁用，1=启用）。例如：安魂曲|1.0|1  可写多行，作用相当于本工作流的“默认启用/权重”。LoRA 的真实文件名、是否仅模型、预设提示词都在上方全局「LoRA 库」里配置，这里只需用名称引用即可；留空则本工作流不默认启用任何 LoRA（仍可用 /draw --名称 临时启用）。即使写了，若标记为 0（禁用）也不会生效。",
            "default": ""
          },
          "workflow_json": {
            "type": "text",
            "description": "直接粘贴 JSON（备选）",
            "hint": "把 ComfyUI 里导出的 API 格式工作流 JSON 粘到这里（与 workflow_name 二选一）。放在最下方避免影响上方字段。",
            "default": ""
          }
        }
      }
    },
    "default": []
  },
  "img2img_fallback": {
    "type": "string",
    "description": "图生图取不到参考图时的处理方式",
    "hint": "判定为图生图、但取不到任何参考图（例如调用方/伴侣插件传入的图片路径在本机不存在）时的行为。可选：prompt（给用户提示重发图，不降级，默认）/ txt2img（回退为文生图，且按原风格回退：真人图生图→真人文生图，动漫图生图→动漫文生图）。",
    "default": "prompt",
    "options": ["prompt", "txt2img"]
  },
  "default_style_priority": {
    "type": "string",
    "description": "未指定工作流时的风格优先级",
    "hint": "用户未明确指定工作流时，默认优先使用「动漫」还是「真人」风格的工作流。可选：anime（动漫优先，默认）/ real（真人优先）。例如设为 real 时，文生图默认先取「真人文生图工作流」，图生图默认先取「真人图生图工作流」。",
    "default": "anime",
    "options": ["anime", "real"]
  },
  "default_workflow": {
    "type": "string",
    "description": "默认动漫文生图工作流名",
    "hint": "用户未指定工作流时，文生图默认使用的「动漫」风格工作流（当风格优先级=anime 时优先）。留空则回退到第一个工作流。",
    "default": ""
  },
  "default_workflow_real": {
    "type": "string",
    "description": "默认真人文生图工作流名",
    "hint": "用户未指定工作流时，文生图默认使用的「真人」风格工作流（当风格优先级=real 时优先）。留空则回退到「默认动漫文生图」。",
    "default": ""
  },
  "default_img2img_workflow": {
    "type": "string",
    "description": "默认动漫图生图工作流名",
    "hint": "用户未指定工作流时，图生图默认使用的「动漫」风格工作流（当风格优先级=anime 时优先）。留空则回退到「默认动漫文生图」。",
    "default": ""
  },
  "default_img2img_workflow_real": {
    "type": "string",
    "description": "默认真人图生图工作流名",
    "hint": "用户未指定工作流时，图生图默认使用的「真人」风格工作流（当风格优先级=real 时优先）。留空则回退到「默认动漫图生图」。",
    "default": ""
  },
  "danbooru": {
    "type": "object",
    "description": "Danbooru 标签服务器",
    "hint": "用于 Anima 工作流翻译中文提示词。只需在用到 Anima 工作流时配置并启用。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用翻译",
        "default": false
      },
      "url": {
        "type": "string",
        "description": "标签服务器地址",
        "hint": "例如 http://127.0.0.1:11111",
        "default": "http://127.0.0.1:11111"
      },
      "api_path": {
        "type": "string",
        "description": "搜索接口路径",
        "default": "/api/search"
      },
      "limit": {
        "type": "int",
        "description": "标签数量上限",
        "slider": {
          "min": 1,
          "max": 100,
          "step": 1
        },
        "default": 20
      },
      "show_nsfw": {
        "type": "bool",
        "description": "包含 NSFW 标签",
        "default": false
      },
      "use_segmentation": {
        "type": "bool",
        "description": "启用分词",
        "default": true
      },
      "popularity": {
        "type": "float",
        "description": "标签流行度阈值",
        "hint": "仅返回流行度不低于该值的标签（0~1），值越高过滤掉的生僻标签越多。",
        "slider": {
          "min": 0,
          "max": 1,
          "step": 0.05
        },
        "default": 0.15
      },
      "top_k": {
        "type": "int",
        "description": "候选标签数量(top_k)",
        "hint": "接口返回的候选标签数量上限。",
        "slider": {
          "min": 1,
          "max": 100,
          "step": 1
        },
        "default": 20
      },
      "append_original": {
        "type": "bool",
        "description": "追加原文",
        "hint": "翻译后是否在标签后追加用户原始中文描述。",
        "default": false
      }
    }
  },
  "translator_mode": {
    "type": "string",
    "description": "Anima 工作流默认提示词翻译模式",
    "hint": "Anima（动漫）工作流提交前，若提示词含中文，按此模式翻译为英文标签。可选：danbooru（Danbooru 标签服务器）/ llm（LLM 翻译，用 translate_llm_model，留空走默认对话模型）/ api（通用 HTTP 翻译接口，见 translate_api 配置）。各 Anima 工作流也可单独覆盖此设置。",
    "default": "danbooru",
    "options": ["danbooru", "llm", "api"]
  },
  "translate_llm_model": {
    "type": "string",
    "description": "LLM 翻译用模型（可选）",
    "hint": "translator_mode=llm 时使用的模型服务商。选填：留空则用 AstrBot 默认对话模型翻译；填入某个 LLM 服务商则用它翻译。",
    "default": "",
    "_special": "select_provider"
  },
  "translate_api": {
    "type": "object",
    "description": "通用 HTTP 翻译接口",
    "hint": "translator_mode=api 时使用的通用翻译接口。可接入任意支持 HTTP 请求的翻译服务（DeepL、百度翻译、自定义中转等）。启用后按下方字段映射提交中文并取回英文标签。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用翻译接口",
        "default": false
      },
      "url": {
        "type": "string",
        "description": "接口地址",
        "hint": "例如 https://api-free.deepl.com/v2/translate 或自定义中转地址。POST 时提交 json/form 到该地址；GET 时把文本拼到 query。",
        "default": ""
      },
      "method": {
        "type": "string",
        "description": "请求方法",
        "hint": "POST（默认）或 GET。",
        "default": "POST",
        "options": ["POST", "GET"]
      },
      "headers": {
        "type": "string",
        "description": "请求头（JSON）",
        "hint": "可选。JSON 格式的额外请求头，如 {\"Authorization\": \"DeepL-Auth-Key xxx\"}。",
        "default": ""
      },
      "text_field": {
        "type": "string",
        "description": "原文字段名",
        "hint": "把中文提示词放进请求体的哪个字段（POST/表单或 GET query）。",
        "default": "text"
      },
      "extra_params": {
        "type": "string",
        "description": "额外固定参数（JSON）",
        "hint": "除原文外的其他请求参数，JSON 格式，随请求体或 query 一起发送。可填接口要求的固定参数，如 {\"target_lang\":\"EN\"}、{\"from\":\"zh\",\"to\":\"en\",\"appid\":\"xxx\",\"salt\":\"1\"} 等。值里可用 {text} 占位符表示原文（例如 {\"q\":\"{text}\"}）。",
        "default": ""
      },
      "json_body": {
        "type": "bool",
        "description": "使用 JSON 请求体",
        "hint": "开启=请求体为 JSON；关闭=使用表单 x-www-form-urlencoded。",
        "default": true
      },
      "result_field": {
        "type": "string",
        "description": "结果字段路径",
        "hint": "英文结果在返回 JSON 里的路径，点分隔取嵌套字段，如 \"data.translated\" 或 \"translated\"。",
        "default": "translated"
      },
      "append_original": {
        "type": "bool",
        "description": "追加原文",
        "hint": "翻译后是否在标签后追加原始中文描述。",
        "default": false
      },
      "timeout": {
        "type": "int",
        "description": "超时(秒)",
        "slider": {
          "min": 5,
          "max": 300,
          "step": 5
        },
        "default": 60
      }
    }
  },
  "enable_llm_tools": {
    "type": "bool",
    "description": "启用 LLM 画图工具",
    "hint": "开启后 AI 对话中可自动调用画图工具（comfyui_draw / comfyui_img2img）。关闭后仅支持指令画图（/draw、/img2img、/画xxx 等），避免与伴侣插件等第三方插件重复触发。",
    "default": true
  },
    "filter_companion_prompt": {
      "type": "bool",
      "description": "伴侣插件提示词过滤（总开关）",
      "hint": "默认关闭——原生调用与伴侣插件调用都**完全原样透传**，不做任何提示词改写。开启后：伴侣插件（带 source 标记）调用时自动做完整过滤（通用拆分清洗 + 专属过滤，抽取用户诉求与构图连续性，过滤时间/日程/位置/情绪等无关事实与 Avoid/Do not 负面约束）；原生调用同样归属此过滤功能，由本开关统一控制。关闭即完全不过滤。",
      "default": false
    },
  "llm_model": {
    "type": "string",
    "description": "指定用于「理解指令并调用工具」的大模型",
    "hint": "选填。当默认对话模型不支持 Function Calling（工具调用）导致 AI 调用 comfyui_draw / comfyui_gallery 等工具时传参为空、出图失败，可在此选择一个支持工具调用的 LLM 服务提供商，插件会用它来解析用户意图并补全工具参数。留空则用 AstrBot 默认对话模型。",
    "default": "",
    "_special": "select_provider"
  },
  "draw_timeout": {
    "type": "int",
    "description": "出图超时(秒)",
    "hint": "等待 ComfyUI 出图的单张基础超时时间。若前面有排队任务，实际等待超时会在此基础上按队列累加（见 queue_extra_timeout）。",
    "slider": {
      "min": 30,
      "max": 600,
      "step": 10
    },
    "default": 120
  },
  "queue_extra_timeout": {
    "type": "int",
    "description": "每队列任务额外超时(秒)",
    "hint": "前面每排队一个任务，等待超时额外累加的秒数。默认取 draw_timeout（保守估算每个任务都要完整基础耗时）。填 0 则用默认值。",
    "slider": {
      "min": 0,
      "max": 600,
      "step": 10
    },
    "default": 0
  },
  "max_draw_timeout": {
    "type": "int",
    "description": "出图超时上限(秒)",
    "hint": "动态超时的封顶值，防止队列过长导致超时无限放大。填 0 则用默认（基础超时的 31 倍）。",
    "slider": {
      "min": 0,
      "max": 7200,
      "step": 60
    },
    "default": 0
  },
  "queue_poll_interval": {
    "type": "int",
    "description": "轮询间隔(秒)",
    "hint": "轮询出图结果的间隔（等待 ComfyUI 生成完成时使用，不再查询队列）。",
    "slider": {
      "min": 1,
      "max": 10,
      "step": 1
    },
    "default": 2
  },
  "return_queue_position": {
    "type": "bool",
    "description": "提交后发送提示",
    "hint": "提交绘图任务后是否发送可爱提示（无排队时提示正在出图，有排队时提示前面还有几位）。",
    "default": true
  },
  "convert_webp_to_png": {
    "type": "bool",
    "description": "发送前把 webp 图片转成 png",
    "hint": "部分适配器（onebot/QQ 等）在 Agent 工具场景下对 webp 图片内联推送失败，可能导致图片没发出来。开启后，出图发送前会用 Pillow 把 webp 转成 png 临时副本再发送（归档仍保留原 webp）。默认关闭。",
    "default": false
  },
  "show_draw_report": {
    "type": "bool",
    "description": "出图完成后输出文件信息",
    "hint": "出图完成后是否发送一条包含尺寸/大小/耗时/时间的小报告。默认关闭（关闭则不输出文件信息）。",
    "default": false
  },
  "allow_draw_users": {
    "type": "text",
    "description": "发图白名单（允许绘图/发图的用户）",
    "hint": "填写允许使用绘图功能（指令绘图、AI 对话发图）的用户 ID，每行一个（也支持逗号分隔）。留空则所有用户都能发图。非白名单用户触发绘图时会收到无权提示且不会真正生图。",
    "default": ""
  },
  "draw_limit": {
    "type": "object",
    "description": "生图次数限制（全局默认）",
    "hint": "限制每个用户的总生图次数、每小时生图次数与每天生图次数。-1 表示不限制。未单独配置的用户使用这里的全局值；某个用户单独配置了则在「限额」页面生效其单独值。到上限时会提示用户。管理员可设为本组 admin_exempt 免除限制。",
    "items": {
      "enabled": {
        "type": "bool",
        "description": "启用生图次数限制",
        "hint": "总开关。关闭后所有用户的生图次数不受限（限额页面仍可查看已生图数量）。",
        "default": false
      },
      "max_total": {
        "type": "int",
        "description": "全局总生图次数上限",
        "hint": "每个用户累计总生图次数上限，超过后无法继续生图。-1 表示不限制。",
        "default": -1,
        "slider": {
          "min": -1,
          "max": 1000,
          "step": 1
        }
      },
      "max_hour": {
        "type": "int",
        "description": "全局每小时生图次数上限",
        "hint": "每个用户 1 小时内生图次数上限，超过后需等到下个整点才能继续。-1 表示不限制。",
        "default": -1,
        "slider": {
          "min": -1,
          "max": 200,
          "step": 1
        }
      },
      "max_day": {
        "type": "int",
        "description": "全局每天生图次数上限",
        "hint": "每个用户当天生图次数上限，超过后需等到当天 0 点重置才能继续。-1 表示不限制。",
        "default": -1,
        "slider": {
          "min": -1,
          "max": 500,
          "step": 1
        }
      },
      "admin_exempt": {
        "type": "bool",
        "description": "管理员不受限制",
        "hint": "开启后管理员（event.is_admin 判定）不参与次数限制，可无限生图。",
        "default": true
      }
    }
  },
  "http_proxy": {
    "type": "string",
    "description": "HTTP/HTTPS 代理（用于 C 站抓取等外部请求）",
    "hint": "插件访问外部网络（如 C 站 civitai.com 抓取封面图/触发词）使用的代理地址，格式如 http://127.0.0.1:7890。留空则仅尝试 AstrBot 全局 http_proxy 与环境变量；若仍无法访问 C 站，在此配置代理后保存即可。",
    "default": ""
  },
  "civitai_api_key": {
    "type": "string",
    "description": "Civitai API Key（可选，提升抓取稳定/避免限流）",
    "hint": "可选填 Civitai 账号生成的 API Token（https://civitai.com/settings 的 Account 页面）。C 站对无 token 的 API 请求可能限流或拿不到完整图片列表，配置后抓取更稳定（请求头带 Authorization: Bearer）。",
    "default": ""
  },
  "gallery": {
    "type": "object",
    "description": "图片画廊与语义标签召回",
    "hint": "把生成的成品图、图生图参考图、用户在聊天里发来/收藏的图，按内容寻址永久归档到 gallery/ 与 refs/，支持按提示词检索和按语义标签（如「合照」）召回。模型不直接接触文件路径，发图由插件在本地完成。",
    "items": {
      "enabled": {
        "type": "bool",
        "default": true,
        "description": "启用图库",
        "hint": "关闭后生成的图与收藏的图不会入库存档，comfyui_gallery 工具与 /gallery 指令均不可用。"
      },
      "max_total_mb": {
        "type": "int",
        "default": 2048,
        "slider": {
          "min": 128,
          "max": 20480,
          "step": 128
        },
        "description": "图库容量上限（MB）",
        "hint": "超出后按 LRU（最早创建优先）淘汰旧图；收藏图与带语义标签的图永不淘汰。"
      },
      "cross_session": {
        "type": "bool",
        "default": false,
        "description": "跨会话检索",
        "hint": "开启后所有会话的图都可被检索；关闭时 /gallery 与 comfyui_gallery 仅能检索「当前会话」范围内生成的图。"
      },
      "keep_temp_hours": {
        "type": "int",
        "default": 24,
        "description": "temp/ 保留小时数",
        "hint": "下载中转目录 temp/ 中副本的保留小时数，到期自动清理。成品图已通过内容寻址移入 gallery/，temp/ 仅作中转。"
      },
      "display_mode": {
        "type": "string",
        "default": "text",
        "options": ["text", "render"],
        "description": "图库展示方式",
        "hint": "列表/搜索/找标签/收藏列表/统计等展示内容的发送方式：text=直接发文字；render=尝试用 AstrBot 渲染服务（text_to_image，Pillow 渲染）把文字渲染成图片发送，渲染失败时自动回退为文字并附提示。要让 render 生效，需在 AstrBot 设置里启用「文本转图片」并选择激活模板。",
        "obvious_hint": true
      },
      "page_size": {
        "type": "int",
        "default": 5,
        "description": "图库每页显示数量",
        "hint": "列表/收藏列表每页展示的条目数。有效范围 1~50，超出会自动夹紧。"
      }
    }
  }
}
